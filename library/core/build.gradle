apply plugin: 'java'

buildscript {
    repositories {
        jcenter()
    }
    dependencies {
    }
}

dependencies {
    compile 'com.squareup.retrofit:retrofit:1.9.0'

    testCompile 'junit:junit:4.12'
}